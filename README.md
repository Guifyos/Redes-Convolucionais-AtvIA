# Projeto de Redes Convolucionais (CNN)
Trabalho de Inteligência Artificial.

## Como rodar o projeto
### Backend (Python)
1. `cd backend`
2. `pip install -r requirements.txt`
3. `uvicorn main:app --reload`

### Frontend (Vite)
1. `cd frontend`
2. `npm install`
3. `npm run dev`